To add new books or members, navigate to './Data/books.txt' or '.Data/books.txt'
Do not remove any Files from ./Data.
new users can also be added through the app
Screens.py contains Strings that are shown in Consol.
Data is both saved as a Python dictionary and as a .txt file, each change is applied to the ditionary and than to the .txt files. 
Dictionary is loaded each time the App is executed. you can clear it from the option '5' inside the app but to clean the history files,
you need to  navigate to '/Data/UserHistory/log' and to '/Data/UserHistory/log' and delete the files, new files will be created automatically.